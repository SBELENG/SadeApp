import React from 'react';

export const LoginPage: React.FC = () => {
  return (
    <div style={{ padding: '20px', color: 'var(--text)' }}>
      <h1>Login Page</h1>
      <p>Bienvenido a SADE. Por favor inicie sesión.</p>
    </div>
  );
};

export default LoginPage;
